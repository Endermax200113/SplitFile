# SplitFile
SplitFile - это программа, которая позволяет дробить огромные файлы на несколько частей. Помимо этого, программа может соединять мелкие файлы в один единый файл.
## Версии
### v0.0.7
- Добавлен класс `Log.cs` в папке `Util`, в котором пишутся вся информация о происходящем в программе;
- Добавлен интерфейс `IException.cs` в папке `Exceptions`, в папке которой будут вставлен обязательный метод `SendMessage()`;
- В классе `AnotherException.cs` добавлен метод для упрощения указания причины ошибки. Теперь не нужно очень подробно писать;
- ???
### v0.0.6
- Исправлена ошибка, из-за которого после переключения на папку назад в обзоре, после клика кнопки в пути происходила ошибка;
- Упрощены обработчики исключений, теперь можно просто вызывать методы `CheckError<>()` и `SendMessage()` с класса `ButtonException`;
- Добавлена несколько классов исключений:
	- `AnotherException.cs` - класс исключения только для отправки сообщений;
	- `FileAndDirException.cs` - исключение связанные с файлом и каталогом;
	- `InitException.cs` - исключение связанное с инициализацией определённого объекта.
- Добавлен вспомогательный класс `FileManager.cs`, который управляет файлами и каталогами;
- Переписаны полностью с нуля все коды в папке `GUI`, т.к. до этого они напрямую управляли с файлами и папками;
- В классе `ButtonException.cs` добавлен новый тип ошибки `ERR_BUTTON_NOT_FOUND`, который говорит о том, что кнопка не была найдена;
- Была запрограммирована кнопка `Обзор` в вкладке `Дробление` в нижнем правом углу.
### v0.0.5
- Добавлен класс `ButtonPath.cs` в папке `GUI`, которые отвечают за путь к папке;
- Были добавлены обработчики событий во время клика в классе `ButtonPath.cs`;
- Запрограммированы кнопки путей, можно вернуться к определённой папке;
- Исправлена ошибка, из-за которого во время обзора, когда не выделен файл, до сих пор была активна кнопка `Дробить файл`.
### v0.0.4
- Добавлена новая обработка исключений, когда нет аргументов событии в кнопке;
- Был добавлен класс `ButtonDirectory.cs` в папке `GUI`, отвечающее за папку;
- Был добавлен класс `ButtonFile.cs` в папке `GUI`, отвечающее за файл;
- Был добавлен класс `PanelDirectory.cs` в папке `GUI`, которые отображают кнопки, отвечающие за папки и файлы;
- Все коды из `FormMain.cs` были перекинуты в другие файлы в папке `GUI`;
- Были запрограммированы кнопки каталогов, которые следуются по папкам.
### v0.0.3
- Был добавлен вспомогательный панель для управления файлов;
- Были деактивированы элементы для управления файлом;
- Была запрогромирована форма загрузки, во время которой загружает начальные кнопки;
- Была добавлена обработка исключений во время возникновения ошибки кнопки.
### v0.0.2
- Были изменены цвета оформления окна;
- Добавлена боковая панель с вкладками `Дробление`, `Слияние` и `Настройки`;
- Были добавлены вспомогательные панели;
- Добавлена нижняя правая кнопка `Обзор`;
- Добавлена кнопка `Дробить файл`;
- Были добавлены информации о дроблении файлов.
### v0.0.1
- Добавлен пакет файлов `MaterialSkin.2` для оформления окон в стиле "Material Design";
- Вставлен необходимый код в `Form1.cs`;
- Были изменены некоторые свойства в файле `Form1.cs`;
- Файл `Form1.cs` был перемещён в папку `Forms` и был переименован в `FormMain.cs`.